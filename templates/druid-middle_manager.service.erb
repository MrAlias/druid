[Unit]
Description=Druid Middle Manager Node

[Service]
Type=simple
WorkingDirectory=/etc/druid/middle_manager/
ExecStart=/usr/bin/java <%= @jvm_opts.join(' ') %> -classpath .:/usr/local/lib/druid/lib/* io.druid.cli.Main server middleManager
Restart=on-failure

[Install]
WantedBy=multi-user.target
