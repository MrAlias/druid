[Unit]
Description=Druid Realtime Node

[Service]
Type=simple
WorkingDirectory=/etc/druid/realtime/
ExecStart=/usr/bin/java <%= @jvm_opts.join(' ') %> -classpath .:/usr/local/lib/druid/lib/* io.druid.cli.Main server realtime
Restart=on-failure

[Install]
WantedBy=multi-user.target
